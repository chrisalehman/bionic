#!/bin/bash
# Fixture enforcer with nothing to remove: advisory only, no blocking path.
# A pointer at it cannot be proven either way — there is no enforcement to
# mutate — and the harness must say so rather than call it discriminating.
echo "advisory"
exit 0
