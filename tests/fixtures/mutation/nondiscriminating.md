# Fixture: a WALL whose pointer proves nothing

Same enforcer as discriminating.md, named explicitly because the pointed-at test
does not follow the `.test` stripping convention back to it. The only difference
between the two fixtures is what the test asserts.

The blocked action must not proceed. [WALL: tests/fixtures/mutation/gate-weak.test.sh, tests/fixtures/mutation/gate.sh]
