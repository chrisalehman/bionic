# Fixture: one example of each of the four classes

A plan must carry a Verification Matrix section. [FORM: tests/fixtures/markers/claimed-block.test.sh]

A push to main must not happen from an agent. [WALL: tests/fixtures/markers/claimed-block.test.sh]

Context spend is recorded per step; the recorder must never block. [INSTRUMENT]

Prefer the simpler solution; complexity must be justified in prose. [UNENFORCED]
