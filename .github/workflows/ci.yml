name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Install shellcheck
        run: sudo apt-get install -y shellcheck
      - name: Syntax check
        run: |
          bash -n claude-bootstrap.sh
          bash -n claude-reset.sh
          bash -n hooks/protect-main.sh
          bash -n hooks/protect-database.sh
      - name: ShellCheck main scripts
        run: shellcheck --severity=warning claude-bootstrap.sh claude-reset.sh
      - name: ShellCheck hooks
        run: shellcheck hooks/protect-main.sh hooks/protect-database.sh

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Install jq
        run: sudo apt-get install -y jq
      - name: Run hook tests
        run: |
          bash hooks/protect-main.test.sh
          if [ -f hooks/protect-database.test.sh ]; then
            bash hooks/protect-database.test.sh
          fi

  test-macos:
    runs-on: macos-latest
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Run hook tests
        run: |
          bash hooks/protect-main.test.sh
          if [ -f hooks/protect-database.test.sh ]; then
            bash hooks/protect-database.test.sh
          fi
